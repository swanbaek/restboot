<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.multicamp.mapper.ReactUserMapper">
<!-- [주의] UserMapper인터페이스의 "패키지명.인터페이스명" 를 namespace에 지정해야 함
	각 element들의 id 값은 메서드명으로 지정해야 한다
 -->
 	<insert id="createUser" parameterType="ReactUser">
	 	<selectKey keyProperty="idx" resultType="_int" order="BEFORE">
	 		select react_member_seq.nextval from dual
	 	</selectKey>
	 	<!-- insert문 완성 -->
	 	insert into react_member
	 	values(#{idx},#{name},#{nickname},#{pwd},'USER',sysdate)
	</insert>
	
	<select id="nickCheck" resultType="int" parameterType="string">
		select idx from react_member where nickname=#{value}
	</select>

	<select id="findUser" resultType="ReactUser" parameterType="ReactUser">
		select * from react_member where nickname=#{nickname}
	</select>
</mapper>
